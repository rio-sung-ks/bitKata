import mongoose from "mongoose";

/*

  TODO: Fill in the model specification

 */
const ProblemSchema = new mongoose.Schema({});

const model = mongoose.model("Problem", ProblemSchema);

export default model;
