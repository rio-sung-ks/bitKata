<!DOCTYPE html>
<html>
  <head>
    <title>BitKata</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1>BitKata</h1>
    <h2>코딩 전쟁이 일어난다. 모든 대원들, 전투 준비!</h2>
    <p style="color: blue; text-decoration: underline"onclick="gitLogin()">로그인 페이지</p>

    <ul class="tabs">
      <select name="dropdown" id="dropdown" onchange="filterProblems()" >
        <option value="all">전체</option>
        <option value="1">Level 1</option>
        <option value="2">Level 2</option>
        <option value="3">Level 3</option>
      </select>
    </ul>

    <ul class="problems">
      <li data-level="1">
        <div id=0 onclick="handleClick(this.id)" style="color: blue; text-decoration: underline" >피보나치 수열</div>
        <div>정답자: 15명</div>
        <div>난이도: Level 1</div>
      </li>
      <li data-level="2">
        <div id=1 onclick="handleClick(this.id)" style="color: blue; text-decoration: underline" >서울에서 김서방 찾기</div>
        <div>정답자: 11명</div>
        <div>난이도: Level 2</div>
      </li>
      <li data-level="2">
        <div id=2 onclick="handleClick(this.id)" style="color: blue; text-decoration: underline" >수박수박수박수박수</div>
        <div>정답자: 12명</div>
        <div>난이도: Level 2</div>
      </li>
      <li data-level="3">
        <div id=3 onclick="handleClick(this.id)" style="color: blue; text-decoration: underline" >하샤드 수</div>
        <div>정답자: 5명</div>
        <div>난이도: Level 3</div>
      </li>
    </ul>
    <script>
        function filterProblems(){
          const selectLevel = document.querySelector("#dropdown").value;
          const problems = document.querySelectorAll(".problems li");

          problems.forEach(problem => {
            const dataLevel = problem.getAttribute("data-level");
            if( selectLevel === "all" || selectLevel === dataLevel){
              problem.style.display = "block";
            } else {
              problem.style.display = "none";
            }
          })
        }


      //   function filterProblems() {
      //   const selectedLevel = document.getElementById("dropdown").value;
      //   const problems = document.querySelectorAll(".problems li");

      //   problems.forEach(problem => {
      //     const level = problem.getAttribute("data-level");
      //     if (selectedLevel === "all" || level === selectedLevel) {
      //       problem.style.display = "block";
      //     } else {
      //       problem.style.display = "none";
      //     }
      //   });
      // }






        function handleClick(problemId){
        // console.log(problemId);
        window.location.href = `/problems/${problemId}`;
      }
      function gitLogin(){
        window.location.href = `/login`;
      }
    </script>
  </body>
</html>
